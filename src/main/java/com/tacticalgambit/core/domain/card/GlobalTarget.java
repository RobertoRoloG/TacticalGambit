package com.tacticalgambit.core.domain.card;

/**
 * Objetivo global para cartas sin objetivo físico en el tablero (ej. robar, ciclar mano, recargar AP).
 */
public record GlobalTarget() implements CardTarget {
}
